---
description: Academic ideation — mine and rank research gaps in a subject (tool-grounded, skeptic-checked GapScore leaderboard)
argument-hint: "<subject> [--intensity standard|intensive|exhaustive] [--window <years>] [--top <k>]"
disable-model-invocation: true
---

Run the `ideation` skill with: $ARGUMENTS

Follow `${CLAUDE_PLUGIN_ROOT}/skills/ideation/SKILL.md` exactly: doctor
preflight, subject scoping + landscape scan, then the phase state machine —
corpus discovery, parallel gap mining, consolidation + verification,
deterministic bibliometric metrics + judge panel + skeptic survival checks,
and a ranked ideation report. Default intensity `standard`, default window 12
years, default leaderboard depth `--top 10`.
